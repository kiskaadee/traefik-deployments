# The domain name where the service will be accessible
SERVICE_DOMAIN=mermaid.example.com

# The name of the external Docker network used by the reverse proxy (e.g., Traefik)
PROXY_NETWORK=proxy-net

# The base name for the service, used in container names and Traefik labels
SERVICE_NAME=mermaid
