services:
  excalidraw:
    image: excalidraw/excalidraw:latest
    environment:
      - REACT_APP_BACKEND_V2_GET_URL=https://${DOMAIN}/api/v2
      - REACT_APP_BACKEND_V2_POST_URL=https://${DOMAIN}/api/v2
      - REACT_APP_WS_SERVER_URL=wss://${DOMAIN}/socket.io
    networks:
      - public
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.excalidraw.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.excalidraw.entrypoints=websecure"
      - "traefik.http.routers.excalidraw.tls=true"
      - "traefik.http.routers.excalidraw.tls.certresolver=${CERT_RESOLVER}"
      - "traefik.http.routers.excalidraw.service=excalidraw-svc"
      - "traefik.http.routers.excalidraw-red.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.excalidraw-red.entrypoints=web"
      - "traefik.http.routers.excalidraw-red.middlewares=https-redirect@docker"

      - "traefik.http.services.excalidraw-svc.loadbalancer.server.port=80"

  excalidraw-room:
    image: excalidraw/excalidraw-room:latest
    restart: on-failure
    networks:
      - public
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.excalidraw-room.rule=Host(`${DOMAIN}`) && PathPrefix(`/socket.io`)"
      - "traefik.http.routers.excalidraw-room.entrypoints=websecure"
      - "traefik.http.routers.excalidraw-room.tls=true"
      - "traefik.http.routers.excalidraw-room.tls.certresolver=${CERT_RESOLVER}"
      - "traefik.http.routers.excalidraw-room.service=excalidraw-room-svc"
      - "traefik.http.routers.excalidraw-room-red.rule=Host(`${DOMAIN}`) && PathPrefix(`/socket.io`)"
      - "traefik.http.routers.excalidraw-room-red.entrypoints=web"
      - "traefik.http.routers.excalidraw-room-red.middlewares=https-redirect@docker"

      - "traefik.http.services.excalidraw-room-svc.loadbalancer.server.port=8080"


networks:
  public:
    name: ${NETWORK_NAME}
    external: true
